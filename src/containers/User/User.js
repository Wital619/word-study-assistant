import React from 'react';

const User = props => {
  return (
    <h1>User page</h1>

    // TODO: add feature to upload file with ukrainian-english words
    // OR: remove user route and add that upload in words route
  );
};

export default User;
